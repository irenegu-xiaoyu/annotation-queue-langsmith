Some modification / addon for the design

Annotate
- User can selecte multiple or the same highlight text in one sentence, but not overlapped. Same highlight text show rubric in stack in the tooltip.
- Feedback of the same rubric in the same entry is unique. The new submitted feedback override the previous one. 
- Clear feedback button clears the feedback in db. 
Reserve
- requeue entry on Skip button click
- requeue entry on user exist without completes or skip
- can add requeue on time out 
View
- show a no more entry in this queue UI if all entries completed.
